# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
widgetbook/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/android/key.properties
/android/app/keystore.jks
/android/app/*.jks
/android/app/*.keystore
/android/build

# iOS Signing
*.p12
*.mobileprovision
*.cer
*.certSigningRequest

# CI/CD Secrets (local development only - CI uses GitHub Secrets)
.env
.env.local
.env.alpha
.env.beta
.env.internal
.env.production
.env.*.backup
service-account*.json
AuthKey_*.p8

# Environment-selected Firebase client identity (never source-controlled).
/android/app/google-services.json
/android/app/src/*/google-services.json
/ios/Runner/GoogleService-Info.plist
/ios/Firebase/**/GoogleService-Info.plist

# flutter_rust_bridge_codegen generated
lib/src/rust

.vscode/
rust
!rust_builder/rust/
!rust_builder/rust/**
/rust_builder/rust/target/
ios/build/
ios/Podfile.lock
rust_builder/cargokit/build_tool/pubspec.lock
android/app/mapping.txt

# Internal notes
gmp.md

# AI tool config (local-only)
.gemini/
.claude/
.agents/
.codex/

# Design system research archive (local only)
lib/design_system/.archive/

# Local-only worktree ergonomics hook (see /tmp/post-checkout.backup or .githooks on main checkout)
.githooks/post-checkout

# Local codebase understanding snapshots
.omm/codebase-lens/

# Test coverage output (generated by `flutter test --coverage`)
coverage/
