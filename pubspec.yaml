name: crypto_mobile_app
description: A comprehensive crypto mobile application

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # 🔥 NEW: For internationalization
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.18.1 # 🔥 NEW: For internationalization

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true # 🔥 NEW: Enable code generation for i18n

  # 🔥 NEW: Assets section
  assets:
    - assets/images/
